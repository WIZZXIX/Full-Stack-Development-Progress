//The Document Object Model (DOM)
// The DOM is a programming interface for web documents. It represents the page 
// so that programs can change the document structure, sytle, and Content.
// In Simple terms the DOM is how javascript interacts with HTML  

//Selecting Elements: 
const id = document.getElementById('some-id') //Selects one element by its unique ID. 
const class_name = document.querySelector('.some-class') //selects the first element that matches a CSS selector. 

//Manipulating Elemets: 
// .textContent: Gets or sets the text content of an element.
// .value: Gets or sets the value of an input element (like <input>, <textarea>).
// .style: Allows you to change CSS properties.
// .addEventListener(): Attaches an event handler (like a "click" or "input" listener) to an element.

